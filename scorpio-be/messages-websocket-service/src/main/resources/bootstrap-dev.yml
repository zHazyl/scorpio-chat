spring:
  cloud:
    config:
      name: commons, messages-websocket
      uri: http://172.31.30.155:8888
      fail-fast: true
      retry:
        max-attempts: 20
        initial-interval: 60000
        max-interval: 30000
spring:
  cloud:
    config:
      name: commons, auth
      uri: http://ec2-3-91-71-127.compute-1.amazonaws.com:8888
      fail-fast: true
      retry:
        max-attempts: 20
        initial-interval: 60000
        max-interval: 10000
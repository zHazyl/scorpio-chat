spring:
  cloud:
    config:
      name: commons, chat
      uri: http://10.114.0.2:8888
      fail-fast: true
      retry:
        max-attempts: 20
        initial-interval: 60000
        max-interval: 10000